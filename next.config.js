/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains:[
      'hidrasport.com.ar',
    ],
  },
  reactStrictMode: true,
  swcMinify: true,
}

module.exports = nextConfig
